import React from 'react'

export default class DataTypeTable extends React.Component {
    render() {
        return (
            <React.Fragment>
                Data table
            </React.Fragment>
        );
    }
}
